import 'package:flutter/widgets.dart';

class LedListInitialView extends StatelessWidget {
  const LedListInitialView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
