import 'package:flutter/material.dart';

class TemperatureInitialView extends StatelessWidget {
  const TemperatureInitialView({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
} 